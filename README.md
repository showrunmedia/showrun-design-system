# Showrun Media — Brand Operating System

> The substrate that makes every Showrun output look, sound, and feel like it came from the same expert team.

This repo is the canonical, machine-readable source for Showrun Media's brand. It compiles a single set of tokens, templates, and content fragments into every output we ship — capabilities deck, website, proposals, resumes, emails, social.

It's organized as a **three-pillar brand operating system**:

1. **Foundation** — design tokens (colour, typography, semantic aliases, image grade) in W3C Design Tokens Community Group format. Geometry is **not** part of the foundation yet — see Version policy
2. **Content** — canonical, reusable copy fragments (bios, methodology pitches, services, case studies, testimonials, FAQ) imported by every template
3. **Orchestration** — protocols and a sibling skill (`showrun-asset-generator`) that turns an intent ("make me a proposal") into a finished, branded artifact


## What v1.0.0 is

**Scope: Showrun Media.** Showrun OS is a separate product with its own register and needs
its own visual system — these tokens do not apply to it.

> **One accent. Everything else neutral. All remaining colour comes from the photography.**

| token | value | notes |
|---|---|---|
| `color.ground.paper` | `#F5F4F1` | light ground |
| `color.ground.charcoal` | `#141416` | dark ground — sits under the photography's black point |
| `color.text.onPaper` | `#000000` | 19.09:1 |
| `color.text.onCharcoal` | `#FFFFFF` | 18.40:1 |
| `color.grey.onPaper` | `#6E6C69` | 4.76:1 |
| `color.grey.onCharcoal` | `#9A9A9A` | 6.54:1 |
| `color.accent.vermilion` | `#FC4C13` | the only accent |

Type is **Switzer** (display/body) and **Martian Mono** (labels). Image treatment is the
Showrun grade — saturation x0.68, black lift +12, warm bias 1.012, all subtractive.

Every colour value was measured rather than chosen by eye; the contrast ratio and the
reasoning are carried on each token.


## What's inside

```
showrun-design-system/
├── tokens/                  W3C DTCG JSON — the single source of truth
│   ├── color.json
│   ├── typography.json
│   ├── image.json
│   └── semantic/
│       ├── light.json
│       └── dark.json
├── build/                   Generated by Style Dictionary — committed for downstream consumers
│   ├── css/tokens.css
│   ├── css/tokens-dark.css
│   ├── json/tokens.json
│   ├── js/tokens.js
│   ├── tailwind/preset.js
│   └── scss/_tokens.scss
├── content/                 Canonical reusable copy
│   ├── identity/
│   ├── positioning/
│   ├── services/
│   ├── case-studies/
│   ├── testimonials/
│   ├── faq/
│   └── content-conventions.md
├── templates/               One folder per asset type — populated through Phases 1-6
│   ├── capabilities-deck/   (Phase 1)
│   ├── proposal-sow/        (Phase 4)
│   ├── resume/              (Phase 5)
│   ├── cover-letter/        (Phase 5)
│   ├── email-signature/     (Phase 6)
│   ├── newsletter/          (Phase 6)
│   └── transactional/       (Phase 6)
├── assets/
│   ├── logos/               SVG + PNG + PDF for media & OS variants
│   ├── fonts/               Switzer, Martian Mono (self-hosted WOFF2)
│   └── photography/         Curated imagery + sourcing rules
├── docs/                    Developer + user guides
├── LEARNINGS.md             Self-improvement log (Pillar 3)
├── ROADMAP.md               Public v1.x backlog
├── CHANGELOG.md             Release history
├── package.json             @showrunmedia/tokens npm package
├── style-dictionary.config.mjs
├── .github/workflows/       CI for token build + release
└── README.md                (this file)
```

## How to consume

### Web (Astro / Next.js / any CSS)
```bash
npm install @showrunmedia/tokens
```
```js
// In your global CSS
@import '@showrunmedia/tokens/css';

// Or for the dark theme overrides
@import '@showrunmedia/tokens/css/dark';

// Tailwind preset
// tailwind.config.js
const showrun = require('@showrunmedia/tokens/tailwind');
module.exports = { presets: [showrun], content: [...] };
```

### Code-generated documents (DOCX / PPTX / PDF)
```js
import tokens from '@showrunmedia/tokens';
// tokens.color.accent.vermilion.value === '#FC4C13'
```

### n8n / agents / non-Node consumers
Fetch the flat JSON directly:
```
https://raw.githubusercontent.com/showrunmedia/showrun-design-system/main/build/json/tokens.json
```

### Claude Design (Anthropic Labs)
Onboard a new Claude Design project against this repo — Claude Design will read tokens, components, and content fragments to maintain brand consistency in any generated visual. Use for:
- Rapid visual prototyping (capabilities deck, brand portal previews)
- Fast-turnaround visuals (social posts, one-pagers, ad creative)
- Anywhere you want canvas-based iteration with the brand system pre-loaded

Export designs back into this repo via the per-template `output/` folders. Optionally export to Canva for collaborative edits.

### Canva, Gamma, Figma
Tokens are mirrored manually to each tool's brand kit until their MCP servers support brand-kit writes. See `docs/SYNCING_DESIGN_TOOLS.md` (v1.1).

## Version policy

- **v1.0.0 — Locked 2026-08-06.** Colour, typography, semantic aliases, image grade and logo
  assets are authoritative. Superseded v0.1 token sets are kept for provenance in `/_archive/`,
  deliberately outside the `tokens/**` build glob.
- **Geometry is not defined, and as of v1.0.1 it is no longer shipped.** `spacing.json`,
  `radius.json`, `elevation.json` and `breakpoints.json` were **removed**. They were v0.1
  first-pass candidates that had never been through an exploration or iteration cycle, yet the
  build emitted them as authoritative — so the repo was contradicting the brand kit, which
  states geometry is undefined. Shipping unvalidated values is worse than shipping none: a
  consumer cannot tell a guess from a decision.

  Two of them were also actively wrong. `elevation` used `rgba(15, 23, 42, …)` — Tailwind's
  slate-900, a **blue** shadow in a system that explicitly retired blue and whose dark ground
  is neutral `#141416`. `breakpoints` described a website that does not exist yet.

  **Geometry gets its own development phase** (see `LEARNINGS.md`). Spacing, radius, elevation
  and breakpoints will be derived — together with layout structure and candidate type
  pairings — and proven across every surface the brand touches before anything is locked. They
  will likely differ by document type: a cue sheet, a deck, a one-sheet and a web page do not
  share one geometry.
- **v1.x — Stable.** Major.minor.patch. Token additions are minor bumps. Breaking changes (removed/renamed tokens) are major bumps. Released quarterly from the LEARNINGS log per `docs/OPERATING_CADENCE.md`.

## Three sources of truth

The brand operates on three sources of truth that work together:

| Source | Lives at | Authoritative for |
|---|---|---|
| **`showrun-brand-guidelines` skill** | `reftech12/de-ecosystem/SKILLS/showrun-brand-guidelines/` (private monorepo) | Strategy, voice, tone, positioning, design intent |
| **This repo** | `showrunmedia/showrun-design-system` (public) | Tokens, templates, content fragments, generated artifacts |
| **`showrun-asset-generator` skill** | `reftech12/de-ecosystem/SKILLS/showrun-asset-generator/` (private monorepo) | Orchestrating the flow: intent → template → content → output |

Each answers a different question. Together they're a complete brand OS.

## Contributing

Solo + agents workflow during v0.x and v1.x. Daniel Esteves approves all changes. Contribution process (when this opens up):
- Open an issue describing the gap or improvement
- Reference any LEARNINGS entries that motivate it
- PR with: (a) token / template / content change, (b) updated docs, (c) added to CHANGELOG.md, (d) noted in next release's roadmap entry

## License

MIT for code and tokens. Brand identity (logos, photography, copy in `content/`) remains the property of Showrun Media. See `LICENSE`.

## Links

- **Showrun Media** — https://showrunmedia.com (coming in Phase 3)
- **Brand portal** — https://design.showrunmedia.com (v1.4)
- **Roadmap** — `ROADMAP.md` in this repo

---

*Built by producers, for producers. The system is the approach and the person. The software is catching up.*
